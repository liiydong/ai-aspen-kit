# 交接文档 — AI 做 Aspen Plus 的能力包

> 面向对象：**下一位接手「用 AI 驱动 Aspen Plus 做化工流程设计」这件事的 AI 或人**。
> 读完这份文档，你应当能在没有额外背景的情况下接手、复现并继续扩展这套工作流。
> 编写日期：2026-09-15

---

## ⚠️ 关于本（公开发行）版本

这份文档描述的是**完整的私有工作环境**。公开发行版出于许可与隐私考虑，做了以下裁剪：

| 本文提到的 | 公开发行版的情况 |
|---|---|
| `02-技能包/`（20 个 Agent Skill） | ❌ **未包含** —— 属上游工具包，请从 [发行页](https://github.com/milklong888/chemical-engineering-skills/releases) 获取 |
| `03-运行时-文本/`（后台、知识图谱、设备引擎） | ❌ **未包含** —— 同上，属上游工具包 |
| `01-资源与来源/` | ❌ **未单独保留** —— 其内容已整合进根目录的 `来源与许可.md` |
| `04-脚本库/` | ✅ 完整包含 |
| `05-案例-烟酰胺10000ta/` | ✅ 包含模型、计算报告与核对记录；**原始论文正文未包含** |

**所有本机绝对路径已替换为占位符**（如 `D:\<化工工作区>`、`C:\Users\<用户名>`），
对照表见 [`来源与许可.md`](./来源与许可.md)。

因此：读本文档时，凡是提到 `02-技能包/` 与 `03-运行时-文本/` 的地方，
请理解为「上游工具包提供的能力」，**其能力描述依然有效**，只是文件不在本仓库。

---

## 0. 一句话说清这是什么

这是一套**让 AI（大模型 Agent）真正去操作 Aspen Plus、并产出可交付工程设计文档**的能力包。
它不是 Aspen 的使用教程，而是「AI 如何在没有 GUI 的情况下读写 Aspen 模型、修复收敛、取数核对、生成论文/报告」的**实战方法 + 工具 + 案例**的集合。

能力包由四部分构成：

| 层 | 内容 | 在本包中的位置 |
|---|---|---|
| 软件层 | Aspen Plus V15（含 COM/Automation 接口） | 本机已装，见 §2 |
| 技能层 | 20 个可复用的 Agent Skill（Markdown 指令集） | `02-技能包/` |
| 引擎层 | 随包的专家后台、知识图谱、设备选型器、MCP 工具 | `03-运行时-文本/` |
| 方法层 | 从实战沉淀的操作方法论与踩坑记录 | `04-脚本库/`、本文档 §5–§7 |
| 案例层 | 烟酰胺 10000 t/a 全流程设计（端到端） | `05-案例-烟酰胺10000ta/` |

---

## 1. 任务背景（这个包从哪来）

本包源自一个真实毕设项目：**年产 10000 吨烟酰胺（维生素 B₃）化学法工艺设计**，要求交付
物料衡算、热量衡算、设备选型、三废治理，并建立覆盖全流程的 Aspen Plus 稳态模型。

整个过程中 AI 承担了：

- 从零搭建 36 单元 / 61 流股 / 15 组分的 Aspen 模型（全程无 GUI，靠 COM + 文件级操作）
- 反复迭代收敛、消除全部警告（20 条 → 0 条）
- 用 Aspen 的计算报告反向核对论文中每一处数字，订正了包括「反应热单位错误导致放热量放大 3.6 倍」在内的多处实质性错误
- 生成最终论文（8 篇 / 4.5 万字）与配套数据报告

**这个包就是把这套"AI 做 Aspen"的打法完整封装出来。**

---

## 2. 运行环境（接手前必读）

### 2.1 已安装软件

| 项 | 版本 / 路径 |
|---|---|
| Aspen Plus | **V15.0**，安装于 `C:\Program Files\AspenTech\Aspen Plus V15.0\` |
| Aspen 引擎 | `...\Engine\xeq\`（COM 调用的就是它） |
| 许可 | 已授权（曾有 `AspenPlusLicenseError.txt` 记录，现已正常） |
| Python（含 COM 依赖） | `D:\<化工运行时>\python-env\Scripts\python.exe`（pywin32 已装） |
| Python（含 openpyxl / python-docx） | `D:\<Python3.10>\python.exe` |
| 工作区 | `D:\<化工工作区>\`（模型、报告、脚本产物） |
| 原始脚本库 | `D:\<化工运行时>\`（200+ 探索脚本） |
| 技能根 | `<技能根>\skills\` |

> **注意**：`D:\<化工运行时>\python-env` 里**没有** openpyxl；`D:\<Python3.10>` 里**有** openpyxl 3.1.5 和 python-docx 1.2.0。
> 取数脚本用后者，COM 脚本用前者。

### 2.2 Aspen COM 调用要点

```python
import win32com.client
app = win32com.client.Dispatch('Apwn.Document')
app.InitFromArchive2(r'D:\path\to\model.bkp')   # 打开
app.Engine.Run2()                                # 运行
val = app.Tree.FindNode(r'\Data\Streams\S-209\Output\MASSFLMX\MIXED').Value
app.SaveAs(r'D:\path\to\out.bkp')                # 另存
app.Close()
```

- 字段路径形如 `\Data\Streams\<流股>\Output\<属性>\MIXED`、`\Data\Blocks\<模块>\...`
- **打开 + 运行一次约 3–10 分钟**（36 单元规模），务必用后台任务跑，不要阻塞对话
- `app.Visible = 0` 可隐藏窗口；`SuppressDialogs` 可抑制弹窗

---

## 3. 资源来源与出处（"这些东西是哪来的"）

| 资源 | 来源 | 说明 |
|---|---|---|
| `chemical-engineering-skills` 发行包 | 用户提供，版本 **0.4.0**，解包于 `D:\<化工运行时>\pkg\chemical-engineering-skills-0.4.0` | 20 个专业技能，含设备选型、塔设计、Aspen 操作、成本估算等 |
| 随包后台与知识图谱 | `D:\<化工工作区>\chemical-engineering-runtime\` | 专家 CLI、知识图谱、设备选型引擎、可选 Aspen MCP 封装 |
| 工作区知识入口 | `D:\<化工工作区>\LOCAL_KNOWLEDGE_GRAPH_LINKS.md` | **原始文件已复制到 `01-资源与来源/`** |
| 安装路径声明 | `D:\<化工工作区>\CHEMICAL_SKILLS_INSTALLATION.json` | 同上 |
| Aspen 安装包 | `D:\<Aspen安装包>\`（本机） | 体积大，未随包；需要时从本机该目录安装 |
| 参考资料（论文/专利/环评） | `C:\Users\<用户名>\<项目原始资料>\` | 瑞邦验收报告、冠润环评、CN121021387A 专利等 |

> 随包后台的统一查询入口：`D:\<化工工作区>\chemical-engineering-runtime\tools\expert_cli.py`
> 用法：`python expert_cli.py --query "<问题>" [--corpus chemical_principles|sun_lanyi|aspen_v10|equipment|equipment_standards]`

---

## 4. 能力清单

### 4.1 技能包（`02-技能包/`，20 个）

**Aspen 操作类（15 个）**

| 技能 | 用途 |
|---|---|
| `aspen-plus-operations` | Aspen 案例 I/O、组分/物性/模块卡片、运行与导出的基础操作 |
| `aspen-bkp-forensics` | **.bkp 文件级"外科手术"**：文本正则改参数、段名对照、COM 写入；本包最核心的技能 |
| `aspen-report-xlsx-parse` | **解析 Aspen 导出的 xlsx 计算报告**（三列切分还原），批量取数核对 |
| `aspen-flowsheet-error-repair` | 流程报错诊断：Control Panel 证据 → 单点变更 → 同案复算 |
| `aspen-document-driven-flowsheet` | 从设计文档驱动搭建流程，分阶段严谨化替换 |
| `aspen-plus-template` | 从组分清单生成可复用的组分模板 |
| `aspen-two-section-flowsheet` | 两段式/分区流程的边界拆分与连接 |
| `aspen-tower-optimization-workflow` | 精馏塔优化与回接（Design Spec、灵敏度分析） |
| `aspen-heat-pump-distillation-replacement` | 热泵精馏替代常规精馏塔 |
| `aspen-edr-rating-delivery` | EDR 换热器严格校核与交付 |
| `aspen-non-reactor-equipment-cost` | 非反应设备成本提取（Economic Evaluation） |
| `aspen-flowsheet-cost-skill-builder` | 为流程建立设备—成本映射 |
| `aspen-kinetics-documentation` | 从文献/卡片写动力学说明文档 |
| `aspen-adaptive-generalization-loop` | 模板自适应泛化（种子案例 → Calculator/Design Spec） |
| `aspen-pressure-pfd-delivery` | 全流程压力拓扑审计与 PFD/LaTeX 交付 |

**化工专业类（5 个）**

| 技能 | 用途 |
|---|---|
| `chemical-engineering-expert` | 总入口：证据驱动的化工推理层，宏观→微观 |
| `chemical-tower-design` | 塔器（板式/填料）工艺计算与文档 |
| `chemical-equipment-selection-audit` | 从 DOCX/PDF 审计设备设计与选型计算 |
| `equipment-design-app` | 无界面设备设计后端（参数推导、选型、标准查询） |
| `sw6-scripted-equipment-design` | SW6 压力容器计算书脚本化 |

### 4.2 运行时与引擎（`03-运行时-文本/`）

- `tools/expert_cli.py` — 专家查询 CLI（联查方法节点 + 设备事实）
- `backends/equipment/` — 设备选型引擎（先计算后选择）
- `backends/process/` — 设备结果回馈工艺模型
- `knowledge/` — 四套知识图谱：化工原理 / 孙兰义 Aspen / Aspen V10 操作手册 / 设备与标准
- `vendor/aspen-mcp-toolkit/` — 可选的 70 工具 Aspen MCP 封装
- 说明：**仅复制了文本类文件**（.md/.py/.json 等）。原始包共 96 MB，含大体积二进制知识载荷，需要完整版请从 `D:\<化工工作区>\chemical-engineering-runtime` 原目录取。

---

## 5. 核心方法论（AI 做 Aspen 的打法）

### 5.1 铁律：改模型的四条路径，优先级从高到低

1. **COM 直接写** —— 最快，但只能改"文档化字段"（模块参数、流股条件）
2. **bkp 文本替换** —— 能改 COM 触达不到的段（如 `SETUP "SIM-OPTIONS"` 的 `NPHASE`）
3. **示例文件对照** —— 拿 Aspen 自带示例 bkp 找到正确写法再照抄
4. **GUI** —— 最后手段（组分表、亨利组分等必须走界面）

> ⚠️ **实测结论**：`FREE_WATER` / 亨利组分（`HENRY-COMPS`）用 COM 直写**会令 Aspen 崩溃**；
> 文本注入要么被忽略、要么损坏文件。**二者必须经 GUI 操作**。

### 5.2 bkp 文本替换的致命陷阱

```python
# 错：静默失效
text = text.replace('BASIS_RR', 'BASIS-RR')      # 字段名其实带连字符

# 对：必须校验替换计数
new, n = re.subn(r'BASIS-RR\s*=\s*3\.0', 'BASIS-RR = 2.0', text)
assert n > 0, '未匹配，字段名或写法有误'
```

**字段名大小写、连字符/下划线必须与 bkp 原文完全一致；替换后务必检查计数，否则会静默失效。**

### 5.3 量化影响后再改：对照实验法

任何"简化假设"（缺二元参数、用 Sep 代替严格塔）都应当**做对照实验量化影响**，再决定是否接受。
本案例中的做法：注入 5 对真实二元参数 → 与理想溶液基线逐项对照 → 得到「关键结果变化 < 0.02%」的结论，
从而证明物料衡算结论不依赖该假设。

### 5.4 收敛与警告的处理原则

- **先分清三类**：① 循环回路首次迭代的零流量旁路（无法也无需消除）；② 相态稳定性警告（用 3-phase 修）；③ 物性荒谬值（如超临界组分用活度系数模型算液相逸度）
- **改一处、跑一次、比一次**，每次只动一个变量，用 Control Panel 原文作证据
- 本案例：全局三相闪蒸（`SETUP "SIM-OPTIONS"` 中 `NPHASE=3`）一次性消掉 14 条 T-301 稳定性警告，**且结果不变**——这是"把物理事实告诉 Aspen"，不是掩盖

### 5.5 数据核对的前置条件：拿到权威数据源

**Aspen 导出的 `Print to Excel` 报告是核对论文数字的唯一权威源。**
解析它有一套固定陷阱（详见 `02-技能包/aspen-report-xlsx-parse/SKILL.md`）：

1. 报告每行被切成 **A/B/C 三列**，`A+B+C` 直接拼接即还原
2. **B→C 切点会吞一个空格** → 数值粘连（`13.9059` + `13.9059` = `13.905913.9059`）
   对策：B 尾不是 `-`/`+` 时补一个空格
3. **组分名里的数字污染取值**（`H2O` 的 2、`3-CP` 的 3）→ 先剥离标签再取数
4. **页首行 `S-101 S-102 …` 才是可靠分页标记**（`STREAM ID` 行会粘连丢 ID）

---

## 6. 踩坑与错误记忆（务必先看）

| # | 坑 | 现象 | 对策 |
|---|---|---|---|
| 1 | bkp 字段名写错 | `re.subn` 返回 0，**不报错**，参数没落库 | 一律检查替换计数 |
| 2 | 反应热单位 | `kmol/h × kJ/mol` = **MJ/h**，被当 kW 用再 ×3.6 → 放热量放大 3.6 倍，连带污染熔盐量/产汽量/蒸汽平衡结论 | **先审量纲**，再谈数值 |
| 3 | COM 写 `FREE_WATER` | Aspen 进程直接崩溃 | 走 GUI |
| 4 | 文本注入 `HENRY-COMPS` | 被忽略，或文件损坏（远程过程调用失败） | 走 GUI：组分 → 亨利组分 |
| 5 | Aspen 泵/压缩机 | `TOP_VFLOW` 全凝器下为 0，要用 `TOP_LFLOW + MOLE_D` | 用定义式换算 |
| 6 | 模型改了但没存 | 优化只跑在临时算例，最终 bkp 仍是旧参数 | 改完必须 `SaveAs` 并**重新打开核对** |
| 7 | 论文与模型不同步 | 论文数字来自旧模型，模型迭代后未回改 | 每次模型定稿后重跑核对流程 |
| 8 | 一页多流股的表格 | 直接按列宽切分会错位 | 用「最后 N 个数值 token」策略 + 剥离标签 |
| 9 | Python 环境混用 | `ChemicalRuntime\python-env` 无 openpyxl | 取数用 `D:\<Python3.10>` |
| 10 | PowerShell 输出捕获 | 有时 stdout 为空 | 一律写文件再用 Read 读，不要依赖 stdout |

---

## 7. 案例：烟酰胺 10000 t/a（`05-案例-烟酰胺10000ta/`）

这是**端到端可复现**的完整案例，包含：

| 文件 | 内容 |
|---|---|
| `00_封面与摘要` … `07_结论参考文献与附录`（md + docx） | 完整毕业论文 8 篇，共 4.5 万字 |
| `数据核对与订正说明.md` | **用 Aspen 报告反向核对论文的完整记录**（含新旧数据对照表） |
| `烟酰胺Aspen模型_定稿v3_无警告.bkp` | 最终模型：36 单元 / 61 流股 / 15 组分 / **0 警告** |
| `ASPEN PLUS-最终版报告-2026.xlsx` | 模型导出的完整计算报告（61 流股全文） |

**案例要点**：

- 工艺路线：3-甲基吡啶气相氨氧化 → 吸收 → 甲苯萃取 → 四塔精馏 → 化学水解 → 中和 → 脱色 → 三效蒸发 → 结晶 → 离心 → 干燥
- 全流程物料平衡闭合偏差 **0.0000%**（进 = 出 = 15006.04 kg/h）
- 产品 1411.64 kg/h = 10163.8 t/a，纯度 99.62 wt%
- 反应器净移热 2.19 MW，熔盐产 1.0 MPa 蒸汽 3.9 t/h

**建议的复现路径**：先用 `aspen-report-xlsx-parse` 解析那份 xlsx，得到全部流股数据；
再对照论文各章，看哪些数字能对上、哪些是设计值（手算）、哪些是模拟值——**这正是本次工作的全部内容**。

---

## 8. 给下一位的建议（未完成事项）

1. **亨利组分未做**：最终模型的全部警告已消除（用户自行经 GUI 设置完成），若你需要重建模型，切记该步骤无法脚本化
2. **两处仍用 Sep 等效**：T-301（萃取，缺 3-CP 液液平衡数据）、T-403（3-CP/4-CP 分离，缺 VLE 数据）。若能补充物性数据，可换成严格塔
3. **离子组分未纳入**：NaOH / H₂SO₄ / Na₂SO₄ 因 NRTL 焓参数不完整而按化学计量单独核算
4. **单元测试缺失**：`04-脚本库/` 里的脚本是探索过程的产物，多数是一次性工具，未做工程化。若要长期维护，建议按功能归类并补 docstring
5. **Aspen 版本**：本包全部经验基于 **V15**。换版本时字段路径可能变化，需重新核对

---

## 9. 目录导航

### 公开发行版（本仓库）

```
ai-aspen-kit/
├── 交接文档.md                 ← 你正在读的文件（先读这个）
├── README.md                   ← 仓库说明与快速开始
├── 来源与许可.md                ← 上游依赖、第三方权利、路径占位符对照
├── LICENSE                     ← MIT
├── 04-脚本库/                   ← 200+ 个实战脚本（COM/解析/诊断/核对）
└── 05-案例-烟酰胺10000ta/        ← 端到端案例：无警告模型 + 计算报告 + 数据核对记录
```

### 完整私有环境（本文档描述的原始形态）

```
ai-aspen-kit/（私有）
├── 交接文档.md
├── README.md
├── 01-资源与来源/               ← 原始资源清单、知识入口、安装声明、上游发行包
├── 02-技能包/                   ← 20 个 Agent Skill（来自上游工具包，未公开）
├── 03-运行时-文本/              ← 专家后台、知识图谱、设备引擎（来自上游工具包，未公开）
├── 04-脚本库/                   ← 200+ 个实战脚本
└── 05-案例-烟酰胺10000ta/        ← 端到端案例：模型 + 报告 + 论文全文
```

> **上游工具包**：[milklong888/chemical-engineering-skills](https://github.com/milklong888/chemical-engineering-skills)
> （本案例使用 v0.4.0，上游当前为 v0.4.9）
